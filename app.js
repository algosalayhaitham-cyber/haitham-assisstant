// app.js - هيثم AI (نسخة شاملة لكل فروع الرياضيات)

document.addEventListener('DOMContentLoaded', function () {
    const form = document.getElementById('chatForm');
    const input = document.getElementById('input');
    const messages = document.getElementById('messages');

    if (!form || !input || !messages) {
        console.warn('⚠️ عناصر المحادثة غير موجودة');
        return;
    }

    // ================================================================
    // 📚 قاعدة المعرفة الشاملة للرياضيات
    // ================================================================
    const KNOWLEDGE_BASE = {
        // ===== 1. المعادلات =====
        'معادلات خطية': {
            keywords: ['معادلة خطية', 'درجة أولى', 'حل معادلة', 'متغيرين', 'مجموعة حل', 'معادلة من الدرجة الأولى'],
            content: `
📚 **المعادلات الخطية (من الدرجة الأولى):**

**الصيغة العامة:** ax + b = 0 (متغير واحد)
ax + by = c (متغيرين)

**طريقة الحل (متغير واحد):**
1. انقل الحدود المتغيرة إلى طرف والأعداد إلى الطرف الآخر.
2. اقسم الطرفين على معامل المتغير.
3. مثال: 2x + 5 = 13 → 2x = 8 → x = 4

**طريقة الحل (متغيرين):**

**طريقة التعويض:**
1. اختر أحد المتغيرين وعبر عنه بدلالة الآخر.
2. عوض في المعادلة الثانية.
3. حل المعادلة الناتجة.
4. عوض لإيجاد المتغير الآخر.

**طريقة الحذف:**
1. رتب المعادلتين على الصورة: ax + by = c
2. اضرب المعادلات في أعداد مناسبة لتساوي معاملات أحد المتغيرين.
3. اجمع أو اطرح المعادلتين لحذف المتغير.
4. حل المعادلة الناتجة.
5. عوض لإيجاد المتغير الآخر.

**مثال:**
حل: x + y = 5, 2x - y = 1
بالحذف: نجمع المعادلتين → 3x = 6 → x = 2
نعوض: 2 + y = 5 → y = 3
الحل: (2, 3)

💡 **نصيحة:** تحقق من الحل بتعويض القيم في المعادلتين الأصليتين.
            `
        },
        'معادلات تربيعية': {
            keywords: ['معادلة تربيعية', 'درجة ثانية', 'مميز', 'جذور', 'دلتا', 'معادلة من الدرجة الثانية'],
            content: `
📚 **المعادلات التربيعية (من الدرجة الثانية):**

**الصيغة العامة:** ax² + bx + c = 0

**طريقة الحل:**
1. **باستخدام المميز (Δ):**
   Δ = b² - 4ac
   - إذا كان Δ > 0 → جذران حقيقيان مختلفان
   - إذا كان Δ = 0 → جذران حقيقيان متساويان
   - إذا كان Δ < 0 → جذران مركبان

2. **قانون الجذرين:**
   x = (-b ± √Δ) / 2a

3. **طريقة التحليل:**
   ابحث عن عددين حاصل ضربهما = ac ومجموعهما = b

**مثال:**
x² - 5x + 6 = 0
التحليل: (x - 2)(x - 3) = 0
الجذران: x = 2, x = 3

💡 **نصيحة:** استخدم المميز لتحديد نوع الجذور قبل الحل.
            `
        },
        'معادلات تكعيبية': {
            keywords: ['معادلة تكعيبية', 'درجة ثالثة', 'جذور معادلة', 'معادلة من الدرجة الثالثة'],
            content: `
📚 **المعادلات التكعيبية (من الدرجة الثالثة):**

**الصيغة العامة:** ax³ + bx² + cx + d = 0

**طرق الحل:**
1. **طريقة التحليل:** إذا كان للمعادلة جذر صحيح، استخدم القسمة التركيبية.
2. **طريقة كاردان:** تستخدم للمعادلات التي ليس لها جذور صحيحة.

**خطوات التحليل:**
1. جرب القيم الصحيحة (قواسم d).
2. إذا وجدت جذراً، استخدم القسمة التركيبية.
3. حل المعادلة التربيعية الناتجة.

**مثال:**
x³ - 6x² + 11x - 6 = 0
جرب x = 1 → 1 - 6 + 11 - 6 = 0
القسمة التركيبية: (x - 1)(x² - 5x + 6) = 0
(x - 1)(x - 2)(x - 3) = 0
الجذور: x = 1, 2, 3
            `
        },
        'معادلات أسية': {
            keywords: ['معادلة أسية', 'أساس', 'أس', 'لوغاريتم', 'حل معادلة أسية'],
            content: `
📚 **المعادلات الأسية:**

**الصيغة:** a^x = b

**طرق الحل:**
1. **إذا كان الأساس متساوياً:**
   a^x = a^y → x = y

2. **باستخدام اللوغاريتمات:**
   a^x = b → x = log_a(b)

**مثال:**
2^x = 8 → 2^x = 2^3 → x = 3

**أو:**
5^x = 125 → 5^x = 5^3 → x = 3

💡 **نصيحة:** حول كلا الطرفين إلى نفس الأساس إن أمكن.
            `
        },
        'معادلات لوغاريتمية': {
            keywords: ['معادلة لوغاريتمية', 'لوغاريتم', 'log', 'ln', 'حل معادلة لوغاريتمية'],
            content: `
📚 **المعادلات اللوغاريتمية:**

**الصيغة:** log_a(x) = b

**طرق الحل:**
1. **باستخدام تعريف اللوغاريتم:**
   log_a(x) = b → x = a^b

2. **إذا كان هناك لوغاريتمان متساويان:**
   log_a(f(x)) = log_a(g(x)) → f(x) = g(x)

3. **بتجميع اللوغاريتمات:**
   log_a(x) + log_a(y) = log_a(xy)

**مثال:**
log_2(x) = 3 → x = 2^3 = 8

**أو:**
log_2(x) + log_2(4) = 5
log_2(4x) = 5 → 4x = 2^5 = 32 → x = 8
            `
        },
        'معادلات مثلثية': {
            keywords: ['معادلة مثلثية', 'جا', 'جتا', 'ظا', 'sin', 'cos', 'tan', 'حل معادلة مثلثية'],
            content: `
📚 **المعادلات المثلثية:**

**الصيغ الأساسية:**
- sin(x) = a → x = sin⁻¹(a) + 2nπ أو x = π - sin⁻¹(a) + 2nπ
- cos(x) = a → x = ±cos⁻¹(a) + 2nπ
- tan(x) = a → x = tan⁻¹(a) + nπ

**مثال:**
sin(x) = 1/2
x = π/6 + 2nπ أو x = 5π/6 + 2nπ

💡 **نصيحة:** تذكر أن الدوال المثلثية دورية، لذا أضف 2nπ للجا والجتا، و nπ للظا.
            `
        },

        // ===== 2. الهندسة =====
        'مساحات ومحيطات': {
            keywords: ['مساحة', 'محيط', 'مساحة مربع', 'مساحة مستطيل', 'مساحة مثلث', 'محيط دائرة'],
            content: `
📚 **قوانين المساحات والمحيطات:**

**المربع:**
- المساحة = الضلع²
- المحيط = 4 × الضلع

**المستطيل:**
- المساحة = الطول × العرض
- المحيط = 2 × (الطول + العرض)

**المثلث:**
- المساحة = (القاعدة × الارتفاع) / 2
- المحيط = مجموع الأضلاع

**الدائرة:**
- المساحة = π × نصف القطر²
- المحيط = 2 × π × نصف القطر

**متوازي الأضلاع:**
- المساحة = القاعدة × الارتفاع

**شبه المنحرف:**
- المساحة = (مجموع القاعدتين × الارتفاع) / 2

💡 **نصيحة:** احفظ هذه القوانين جيداً، فهي أساس الهندسة.
            `
        },
        'نظرية فيثاغورس': {
            keywords: ['فيثاغورس', 'نظرية فيثاغورس', 'وتر', 'مثلث قائم', 'أضلاع مثلث'],
            content: `
📚 **نظرية فيثاغورس:**

**النص:** في المثلث القائم الزاوية، مربع الوتر يساوي مجموع مربعي الضلعين الآخرين.

**الصيغة:** a² + b² = c²

حيث c هو الوتر (الضلع المقابل للزاوية القائمة).

**تطبيقات:**
- إيجاد طول ضلع مجهول في مثلث قائم.
- إثبات أن المثلث قائم الزاوية.

**مثال:**
مثلث أضلاعه: 3، 4، 5
3² + 4² = 9 + 16 = 25 = 5²
إذن المثلث قائم الزاوية.

💡 **نصيحة:** تستخدم النظرية فقط في المثلثات القائمة الزاوية.
            `
        },
        'المتجهات': {
            keywords: ['متجه', 'متجهات', 'جمع متجهات', 'ضرب متجهات', 'إحداثيات متجه', 'مركبات متجه'],
            content: `
📚 **المتجهات:**

**تعريف المتجه:** كمية لها مقدار واتجاه.

**مركبات المتجه:**
- في المستوى: v = (x, y)
- في الفراغ: v = (x, y, z)

**عمليات على المتجهات:**
1. **الجمع:** (x₁, y₁) + (x₂, y₂) = (x₁+x₂, y₁+y₂)
2. **الطرح:** (x₁, y₁) - (x₂, y₂) = (x₁-x₂, y₁-y₂)
3. **الضرب في عدد:** k(x, y) = (kx, ky)

**الطول (المقدار):**
|v| = √(x² + y²)

**الضرب النقطي:**
v·w = x₁x₂ + y₁y₂

💡 **نصيحة:** المتجهات أساسية في الفيزياء والرياضيات التطبيقية.
            `
        },

        // ===== 3. التفاضل والتكامل =====
        'قواعد الاشتقاق': {
            keywords: ['اشتقاق', 'مشتقة', 'قواعد الاشتقاق', 'تفاضل', 'مشتقة دالة'],
            content: `
📚 **قواعد الاشتقاق الأساسية:**

**1. مشتقة الثابت:** d/dx(c) = 0

**2. مشتقة x^n:** d/dx(x^n) = n·x^(n-1)

**3. مشتقة الجمع والطرح:** 
d/dx(f ± g) = f' ± g'

**4. مشتقة الضرب (قاعدة الضرب):**
d/dx(f·g) = f'·g + f·g'

**5. مشتقة القسمة (قاعدة القسمة):**
d/dx(f/g) = (f'·g - f·g') / g²

**6. مشتقة السلسلة (قاعدة السلسلة):**
d/dx(f(g(x))) = f'(g(x)) · g'(x)

**مشتقات الدوال المثلثية:**
- d/dx(sin x) = cos x
- d/dx(cos x) = -sin x
- d/dx(tan x) = sec² x
- d/dx(sec x) = sec x · tan x

💡 **نصيحة:** احفظ هذه القواعد، فهي أساس التفاضل.
            `
        },
        'التكامل': {
            keywords: ['تكامل', 'قواعد التكامل', 'تكامل غير محدود', 'تكامل محدود', 'مساحة تحت المنحنى'],
            content: `
📚 **قواعد التكامل الأساسية:**

**1. تكامل x^n:** ∫x^n dx = x^(n+1)/(n+1) + C

**2. تكامل الثابت:** ∫c dx = cx + C

**3. تكامل الجمع والطرح:** ∫(f ± g) dx = ∫f dx ± ∫g dx

**4. تكامل الدوال المثلثية:**
- ∫sin x dx = -cos x + C
- ∫cos x dx = sin x + C
- ∫sec² x dx = tan x + C

**التكامل المحدود:**
∫[a,b] f(x) dx = F(b) - F(a)
حيث F هي المشتقة العكسية لـ f.

**تطبيقات التكامل:**
- حساب المساحة تحت المنحنى.
- حساب الحجوم.
- حساب طول المنحنى.

💡 **نصيحة:** التكامل هو العملية العكسية للاشتقاق.
            `
        },

        // ===== 4. الإحصاء والاحتمالات =====
        'مقاييس النزعة المركزية': {
            keywords: ['وسط حسابي', 'وسيط', 'منوال', 'نزعة مركزية', 'إحصاء', 'متوسط حسابي'],
            content: `
📚 **مقاييس النزعة المركزية:**

**1. الوسط الحسابي (المتوسط):**
x̄ = (x₁ + x₂ + ... + x_n) / n

**2. الوسيط:**
- رتب البيانات تصاعدياً.
- إذا كان عدد البيانات فردياً: الوسيط هو القيمة الوسطى.
- إذا كان عدد البيانات زوجياً: الوسيط = متوسط القيمتين الوسطيتين.

**3. المنوال (الموضة):**
- القيمة الأكثر تكراراً في البيانات.

**مثال:**
البيانات: 2, 5, 7, 7, 9, 12, 15
- الوسط: (2+5+7+7+9+12+15)/7 = 57/7 ≈ 8.14
- الوسيط: 7 (القيمة الرابعة)
- المنوال: 7 (تكررت مرتين)

💡 **نصيحة:** استخدم هذه المقاييس لتحليل البيانات.
            `
        },
        'الاحتمالات': {
            keywords: ['احتمال', 'قوانين الاحتمال', 'تجربة عشوائية', 'فضاء العينة', 'حدث', 'احتمال وقوع'],
            content: `
📚 **الاحتمالات:**

**تعريف الاحتمال:** احتمال وقوع حدث = عدد النواتج المواتية / عدد النواتج الممكنة.

**قوانين الاحتمال:**
1. **احتمال الحدث الأكيد:** P(S) = 1
2. **احتمال الحدث المستحيل:** P(∅) = 0
3. **قاعدة الجمع:** P(A ∪ B) = P(A) + P(B) - P(A ∩ B)
4. **قاعدة الضرب (لأحداث مستقلة):** P(A ∩ B) = P(A) × P(B)
5. **الاحتمال الشرطي:** P(A|B) = P(A ∩ B) / P(B)

**مثال:**
رمي قطعة نقود مرتين:
- احتمالية الحصول على صورتين = 1/4
- احتمالية الحصول على صورة وكتابة = 1/2

💡 **نصيحة:** الاحتمالات تستخدم في التنبؤ وتحليل البيانات.
            `
        },

        // ===== 5. المصفوفات =====
        'المصفوفات': {
            keywords: ['مصفوفة', 'مصفوفات', 'جمع مصفوفات', 'ضرب مصفوفات', 'محدد مصفوفة', 'معكوس مصفوفة'],
            content: `
📚 **المصفوفات:**

**تعريف المصفوفة:** ترتيب مستطيل من الأعداد.

**عمليات المصفوفات:**
1. **الجمع:** اجمع العناصر المتناظرة (نفس الأبعاد).
2. **الطرح:** اطرح العناصر المتناظرة (نفس الأبعاد).
3. **الضرب بعدد:** اضرب كل عنصر بالعدد.
4. **ضرب المصفوفات:** 
   - الشرط: عدد أعمدة الأولى = عدد صفوف الثانية.
   - الناتج: (م × ن) × (ن × ل) → (م × ل)

**محدد المصفوفة (2×2):**
| a  b |
| c  d | = ad - bc

**معكوس المصفوفة (2×2):**
A⁻¹ = (1/det(A)) × [d, -b; -c, a]

💡 **نصيحة:** المصفوفات تستخدم في حل أنظمة المعادلات وفي الرسوميات.
            `
        },

        // ===== 6. المتتاليات والمتسلسلات =====
        'متتاليات': {
            keywords: ['متتالية', 'متتالية حسابية', 'متتالية هندسية', 'حد عام', 'مجموع متتالية', 'متسلسلة'],
            content: `
📚 **المتتاليات والمتسلسلات:**

**المتتالية الحسابية:**
- الحد العام: a_n = a₁ + (n-1)d
- حيث d هو الفرق المشترك.
- مجموع أول n حد: S_n = (n/2)(2a₁ + (n-1)d)

**المتتالية الهندسية:**
- الحد العام: a_n = a₁ × r^(n-1)
- حيث r هو الأساس المشترك.
- مجموع أول n حد (r≠1): S_n = a₁(1-r^n)/(1-r)

**مجموع متتالية هندسية لا نهائية (|r|<1):**
S∞ = a₁/(1-r)

**مثال (حسابية):**
a₁ = 2, d = 3 → 2, 5, 8, 11, ...
a₅ = 2 + 4×3 = 14

**مثال (هندسية):**
a₁ = 3, r = 2 → 3, 6, 12, 24, ...
a₄ = 3 × 2³ = 24

💡 **نصيحة:** المتتاليات أساسية في التحليل والتفاضل.
            `
        }
    };

    // ================================================================
    // 🔍 دوال البحث والإجابة
    // ================================================================
    
    function searchKnowledge(query) {
        if (!query) return null;
        const lowerQuery = query.toLowerCase();
        let bestMatch = null;
        let highestScore = 0;

        for (const [topic, data] of Object.entries(KNOWLEDGE_BASE)) {
            let score = 0;
            for (const keyword of data.keywords) {
                if (lowerQuery.includes(keyword)) {
                    score += 2;
                }
            }
            if (data.content.toLowerCase().includes(lowerQuery)) {
                score += 1;
            }
            if (score > highestScore) {
                highestScore = score;
                bestMatch = data.content;
            }
        }

        return highestScore >= 2 ? bestMatch : null;
    }

    // ================================================================
    // 💬 دوال المحادثة
    // ================================================================

    function loadChatHistory() {
        const saved = localStorage.getItem('haitham_chat_history');
        if (saved) {
            try {
                const history = JSON.parse(saved);
                messages.innerHTML = history.map(msg => 
                    `<div class="msg ${msg.role}"><b>${msg.role === 'user' ? 'أنت' : 'هيثم AI'}</b><p>${msg.text}</p></div>`
                ).join('');
            } catch (e) {}
        }
    }
    loadChatHistory();

    function saveChatHistory() {
        const items = [];
        document.querySelectorAll('.msg').forEach(msg => {
            const role = msg.classList.contains('user') ? 'user' : 'ai';
            const text = msg.querySelector('p')?.textContent || '';
            items.push({ role, text });
        });
        localStorage.setItem('haitham_chat_history', JSON.stringify(items));
    }

    function getHaithamReply(message) {
        // 1. البحث في قاعدة المعرفة
        const knowledge = searchKnowledge(message);
        if (knowledge) {
            return knowledge + '\n\n💡 هذه المعلومات مأخوذة من ملخصات هيثم AI.';
        }

        const lower = message.toLowerCase();
        
        if (lower.includes('السلام') || lower.includes('مرحب')) {
            return '👋 وعليكم السلام! كيف يمكنني مساعدتك اليوم؟';
        }
        if (lower.includes('رياضيات')) {
            return '📐 أسألني عن أي موضوع في الرياضيات: معادلات، هندسة، تفاضل، تكامل، إحصاء، مصفوفات، أو متتاليات.';
        }
        if (lower.includes('شكر')) {
            return '🌟 العفو! أنا هنا لمساعدتك دائماً.';
        }
        if (lower.includes('كيف حال')) {
            return '😊 أنا بخير، شكراً لسؤالك! كيف يمكنني مساعدتك اليوم؟';
        }
        if (lower.includes('اسم')) {
            return '🧠 أنا هيثم AI، مساعدك التعليمي الذكي!';
        }
        
        const generalReplies = [
            '📚 شكراً لسؤالك! أنا هنا لمساعدتك.\n\n💡 ما الذي تريد معرفته بالضبط؟',
            '🧠 سؤال جميل! \n\n📝 اشرح لي أكثر وسأجيبك بأفضل طريقة.',
            '🌟 ممتاز! \n\n💪 تذكر أن الممارسة المستمرة هي مفتاح النجاح.'
        ];
        return generalReplies[Math.floor(Math.random() * generalReplies.length)];
    }

    function sendMessage(message) {
        if (!message || !message.trim()) return;
        
        const userMsg = document.createElement('div');
        userMsg.className = 'msg user';
        userMsg.innerHTML = `<b>أنت</b><p>${message}</p>`;
        messages.appendChild(userMsg);
        messages.scrollTop = messages.scrollHeight;
        saveChatHistory();
        
        const typing = document.createElement('div');
        typing.className = 'msg ai';
        typing.id = 'typing';
        typing.innerHTML = `<b>هيثم AI</b><p>⏳ جاري التفكير...</p>`;
        messages.appendChild(typing);
        messages.scrollTop = messages.scrollHeight;
        
        setTimeout(() => {
            const typingEl = document.getElementById('typing');
            if (typingEl) typingEl.remove();
            
            const reply = getHaithamReply(message);
            const aiMsg = document.createElement('div');
            aiMsg.className = 'msg ai';
            aiMsg.innerHTML = `<b>هيثم AI</b><p>${reply.replace(/\n/g, '<br>')}</p>`;
            messages.appendChild(aiMsg);
            messages.scrollTop = messages.scrollHeight;
            saveChatHistory();
        }, 800 + Math.random() * 700);
    }

    // ================================================================
    // 🎯 معالجة الإدخال
    // ================================================================

    form.addEventListener('submit', function(e) {
        e.preventDefault();
        const message = input.value.trim();
        if (!message) return;
        input.value = '';
        sendMessage(message);
    });

    const sendBtn = document.querySelector('.btn-send');
    if (sendBtn) {
        sendBtn.addEventListener('click', function(e) {
            e.preventDefault();
            const message = input.value.trim();
            if (!message) return;
            input.value = '';
            sendMessage(message);
        });
    }

    input.addEventListener('keypress', function(e) {
        if (e.key === 'Enter' && !e.shiftKey) {
            e.preventDefault();
            const message = this.value.trim();
            if (!message) return;
            this.value = '';
            sendMessage(message);
        }
    });

    console.log('✅ هيثم AI جاهز! قاعدة المعرفة تشمل جميع فروع الرياضيات.');
});
